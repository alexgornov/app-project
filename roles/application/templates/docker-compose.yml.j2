---
version: '3'
services:
  app:
    container_name: app
    image: {{ app_image }}:{{ app_tag }}
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge